import React from 'react';

const Chatbot = () => {
    // Your chatbot component code goes here

    return (
        <div>
            <input type="text" placeholder="Enter your message" />
            <button>Send</button>
        </div>
    );
};

export default Chatbot;